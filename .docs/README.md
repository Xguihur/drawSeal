# 印章生成系统 - 项目设计文档

> 文档创建时间：2026-02-02  
> 项目状态：需求确认阶段

---

## 📝 项目概述

本项目旨在开发一个印章生成系统，支持前端配置参数生成印章 PNG 图片，并提供实时预览功能。未来将扩展 Node.js 接口，支持批量生成并上传至 OSS。

### 核心功能

- ✅ 前端配置印章参数（文字、尺寸、颜色等）
- ✅ 实时预览印章效果
- ✅ 导出 PNG 格式图片
- 🔜 Node.js API 接口生成印章
- 🔜 自动上传至 OSS 存储

---

## 🎯 MVP 需求范围

### 第一阶段确定范围

| 功能项 | 状态 | 说明 |
|-------|------|------|
| 印章类型 | ✅ 确定 | 仅支持**公章**（圆形） |
| 配置方式 | ✅ 确定 | 参数配置面板 |
| 预览功能 | ✅ 确定 | 实时预览 |
| 导出功能 | ✅ 确定 | 导出 PNG 格式 |
| 自定义 Logo | ❌ 暂不支持 | 后续版本考虑 |
| 拖拽编辑 | ❌ 不需要 | 仅配置参数方式 |
| 并发优化 | ⏳ 后续考虑 | MVP 不处理，未来可能达到 1w 条/天 |

### 核心用户流程

```
用户进入页面 
  ↓
配置印章参数（文字、尺寸、颜色等）
  ↓
实时预览效果
  ↓
点击导出按钮
  ↓
下载 PNG 图片
```

---

## 🏗️ 技术架构设计

### 整体架构

```
┌─────────────────────────────────────────┐
│           前端 React 应用                │
│  ┌─────────────┐      ┌──────────────┐  │
│  │  配置面板   │ ←──→ │  印章画布     │  │
│  └─────────────┘      └──────────────┘  │
│         ↓                     ↓          │
│  ┌─────────────────────────────────┐   │
│  │      Fabric.js 渲染引擎         │   │
│  └─────────────────────────────────┘   │
│                ↓                        │
│  ┌─────────────────────────────────┐   │
│  │       导出 PNG 功能              │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
                 ↓
        （未来扩展）
┌─────────────────────────────────────────┐
│        Node.js API 服务                  │
│  ┌─────────────────────────────────┐   │
│  │     node-canvas 生成印章         │   │
│  └─────────────────────────────────┘   │
│                ↓                        │
│  ┌─────────────────────────────────┐   │
│  │      上传至 OSS 存储             │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 🛠️ 技术选型

### 前端技术栈

| 技术 | 选型 | 理由 |
|------|------|------|
| **框架** | React | 项目要求 |
| **构建工具** | Vite | 快速开发，热更新体验好 |
| **Canvas 库** | Fabric.js | 功能强大，支持文字弧形排列 |
| **UI 组件** | Ant Design | 快速搭建配置面板 |
| **颜色选择器** | react-color | 颜色配置 |
| **图片导出** | html2canvas / fabric 内置 | 导出 PNG |
| **文件下载** | file-saver | 前端下载文件 |

### 后端技术栈（未来）

| 技术 | 选型 | 理由 |
|------|------|------|
| **框架** | Express.js | 轻量级，易上手 |
| **Canvas** | node-canvas | 服务端 Canvas，API 兼容浏览器 |
| **OSS SDK** | ali-oss | 阿里云 OSS JavaScript SDK |
| **图片处理** | Sharp（备选） | 高性能图片处理 |

---

## 📐 前端架构设计

### 目录结构

```
print-seal/
├── src/
│   ├── components/
│   │   ├── SealCanvas/           # 印章画布组件
│   │   │   ├── index.jsx
│   │   │   └── index.module.css
│   │   ├── SealPreview/          # 预览组件
│   │   │   ├── index.jsx
│   │   │   └── index.module.css
│   │   ├── ConfigPanel/          # 配置面板
│   │   │   ├── index.jsx
│   │   │   ├── BasicConfig.jsx   # 基础配置
│   │   │   ├── TextConfig.jsx    # 文字配置
│   │   │   └── index.module.css
│   │   └── ExportButton/         # 导出按钮
│   │       └── index.jsx
│   ├── utils/
│   │   ├── sealGenerator.js      # 印章生成核心逻辑
│   │   ├── textPath.js           # 文字弧形排列算法
│   │   └── exportImage.js        # 图片导出工具
│   ├── hooks/
│   │   └── useSealConfig.js      # 印章配置状态管理
│   ├── constants/
│   │   └── sealTemplates.js      # 印章模板预设
│   ├── App.jsx
│   └── main.jsx
├── public/
│   └── fonts/                     # 字体文件
│       └── SimSun.ttf
├── package.json
└── vite.config.js
```

### 核心组件职责

#### 1. SealCanvas（印章画布）
- 使用 Fabric.js 渲染印章
- 接收配置参数，动态生成印章
- 提供导出接口

#### 2. ConfigPanel（配置面板）
- 印章文字输入
- 尺寸、颜色、字体选择
- 五角星配置
- 实时触发重新渲染

#### 3. SealPreview（预览组件）
- 展示当前印章效果
- 背景透明/白色切换
- 缩放查看

#### 4. ExportButton（导出按钮）
- 调用 Canvas 导出方法
- 生成 PNG 文件
- 触发浏览器下载

---

## 🎨 印章配置参数设计

### 数据结构

```javascript
const sealConfig = {
  // === 基础信息 ===
  type: 'circle',                  // 印章类型（暂时只有 circle）
  
  // === 尺寸 ===
  diameter: 200,                   // 直径（px）
  borderWidth: 3,                  // 边框宽度（px）
  
  // === 颜色 ===
  color: '#CC0000',                // 印章红色
  
  // === 外圈文字 ===
  outerText: '某某公司',           // 外圈文字内容
  outerFontSize: 24,               // 外圈字体大小
  outerFontFamily: '宋体',         // 字体
  outerTextSpacing: 12,            // 字间距
  outerTextArc: 240,               // 文字弧度（度数）
  
  // === 内圈文字（可选） ===
  innerText: '',                   // 内圈文字（如：合同专用章）
  innerFontSize: 18,
  innerTextArc: 180,
  
  // === 中心图案 ===
  centerType: 'star',              // 'star' 五角星 | 'text' 文字
  centerStarSize: 40,              // 五角星大小
  centerText: '印',                // 中心文字（如果选择文字）
  centerTextSize: 30,
  
  // === 高级选项 ===
  rotation: 0,                     // 整体旋转角度
  opacity: 1.0,                    // 透明度
  
  // === 导出配置 ===
  exportScale: 2,                  // 导出倍数（高清）
  exportFormat: 'png'              // 导出格式
}
```

### 配置面板分组

```
┌─────────────────────────┐
│   基础配置               │
│   - 印章名称             │
│   - 尺寸                 │
│   - 边框粗细             │
│   - 颜色                 │
├─────────────────────────┤
│   外圈文字               │
│   - 文字内容             │
│   - 字体大小             │
│   - 字间距               │
│   - 弧度                 │
├─────────────────────────┤
│   中心图案               │
│   - 五角星 / 文字        │
│   - 大小                 │
├─────────────────────────┤
│   预览 & 导出            │
│   - 实时预览             │
│   - 导出 PNG 按钮        │
└─────────────────────────┘
```

---

## 🔧 核心技术实现要点

### 1. 文字弧形排列算法

公章的外圈文字需要沿着圆弧排列，核心算法：

```javascript
// 伪代码
function arrangeTextOnCircle(text, radius, startAngle, arcAngle) {
  const chars = text.split('');
  const angleStep = arcAngle / (chars.length - 1);
  
  chars.forEach((char, index) => {
    const angle = startAngle + angleStep * index;
    const x = centerX + radius * Math.cos(angle);
    const y = centerY + radius * Math.sin(angle);
    const rotation = angle + 90; // 字符旋转角度
    
    drawChar(char, x, y, rotation);
  });
}
```

### 2. 五角星绘制

使用 Fabric.js 的 Polygon 或自定义路径：

```javascript
// 五角星顶点计算
function getStarPoints(size) {
  const points = [];
  const outerRadius = size;
  const innerRadius = size * 0.382; // 黄金比例
  
  for (let i = 0; i < 10; i++) {
    const radius = i % 2 === 0 ? outerRadius : innerRadius;
    const angle = (Math.PI / 5) * i - Math.PI / 2;
    points.push({
      x: radius * Math.cos(angle),
      y: radius * Math.sin(angle)
    });
  }
  return points;
}
```

### 3. 高清导出

导出时使用 2x 或 3x 倍数，确保清晰度：

```javascript
function exportSeal(canvas, scale = 2) {
  const dataURL = canvas.toDataURL({
    format: 'png',
    quality: 1,
    multiplier: scale  // 2 倍高清
  });
  
  // 触发下载
  saveAs(dataURL, 'seal.png');
}
```

---

## 🚀 未来扩展：Node.js API 设计

### API 接口设计

#### 生成印章接口

```
POST /api/seal/generate
Content-Type: application/json

Request Body:
{
  "config": {
    // 印章配置参数（与前端一致）
    "outerText": "某某科技有限公司",
    "diameter": 200,
    "color": "#CC0000",
    ...
  },
  "options": {
    "upload": true,        // 是否上传 OSS
    "quality": 90,         // 图片质量
    "scale": 2             // 高清倍数
  }
}

Response:
{
  "success": true,
  "data": {
    "imageUrl": "https://oss.example.com/seals/2026/02/seal_xxx.png",
    "ossPath": "seals/2026/02/seal_xxx.png",
    "size": 45678,
    "createdAt": "2026-02-02T10:30:00Z"
  }
}
```

### 后端实现要点

#### 1. node-canvas 使用

```javascript
const { createCanvas } = require('canvas');
const canvas = createCanvas(400, 400);
const ctx = canvas.getContext('2d');

// 绘制印章逻辑（复用前端逻辑）
drawSeal(ctx, config);

// 导出 Buffer
const buffer = canvas.toBuffer('image/png');
```

#### 2. OSS 上传流程

```javascript
const OSS = require('ali-oss');

async function uploadToOSS(buffer, filename) {
  const client = new OSS({
    region: 'oss-cn-hangzhou',
    accessKeyId: process.env.OSS_ACCESS_KEY_ID,
    accessKeySecret: process.env.OSS_ACCESS_KEY_SECRET,
    bucket: 'your-bucket'
  });
  
  // 生成路径：seals/2026/02/02/seal_timestamp.png
  const date = new Date();
  const path = `seals/${date.getFullYear()}/${date.getMonth()+1}/${date.getDate()}/${filename}`;
  
  const result = await client.put(path, buffer);
  return result.url;
}
```

#### 3. 性能优化考虑（未来 1w/天）

- **并发控制**：使用队列（Bull、Bee-Queue）
- **缓存策略**：相同配置缓存结果
- **资源池**：Canvas 实例复用
- **CDN 加速**：OSS + CDN
- **异步处理**：生成任务异步化

---

## 📋 OSS 存储注意事项

### 1. 安全性

- ❌ 前端不存储 OSS 密钥
- ✅ 后端使用 STS 临时授权
- ✅ 设置防盗链、IP 白名单
- ✅ 上传大小限制（如 5MB）

### 2. 文件管理

```
文件命名规则：
  seal_{userId}_{timestamp}_{random}.png

目录结构：
  seals/
    ├── 2026/
    │   ├── 02/
    │   │   ├── 02/
    │   │   │   ├── seal_u001_1738483200_abc.png
    │   │   │   └── seal_u002_1738483300_def.png
```

### 3. 生命周期管理

- 设置临时文件自动清理（如 30 天）
- 正式文件长期保存
- 定期归档冷数据

### 4. 成本优化

- 开启 OSS 智能分层存储
- 图片压缩（保证质量前提下）
- 监控流量和存储用量
- 合理设置 CDN 缓存策略

---

## 📅 实施计划

### 第一阶段：前端 MVP（预计 3-5 天）

**Day 1-2：项目搭建 + 基础功能**
- [ ] 初始化 React + Vite 项目
- [ ] 安装依赖（Fabric.js、Ant Design 等）
- [ ] 搭建基础页面布局
- [ ] 实现简单圆形绘制

**Day 3-4：核心功能**
- [ ] 实现文字弧形排列算法
- [ ] 绘制五角星
- [ ] 完整的印章渲染逻辑
- [ ] 配置面板开发

**Day 5：优化 + 导出**
- [ ] 实时预览优化
- [ ] 导出 PNG 功能
- [ ] 样式美化
- [ ] 基础测试

### 第二阶段：功能完善（预计 2-3 天）

- [ ] 增加预设模板
- [ ] 字体选择功能
- [ ] 配置参数校验
- [ ] 响应式适配
- [ ] 用户体验优化

### 第三阶段：后端服务（暂缓，根据需求启动）

- [ ] 搭建 Express 项目
- [ ] 集成 node-canvas
- [ ] 实现生成接口
- [ ] OSS 上传功能
- [ ] API 文档编写
- [ ] 压力测试

---

## 🔍 技术难点预判

### 1. 文字弧形排列
**难度：⭐⭐⭐**
- 需要精确的三角函数计算
- 不同字数的间距调整
- 中文字符宽度不一致问题

**解决方案：**
- 使用 Fabric.js 的 `Path` 和 `Text.setPathStartOffset`
- 或自定义算法逐字符计算位置

### 2. 中文字体处理
**难度：⭐⭐⭐⭐**
- 前端需要加载字体文件（宋体 ~5MB）
- 后端 node-canvas 字体注册
- 不同系统字体兼容性

**解决方案：**
- 前端使用 Web Font Loader
- 后端预装常用中文字体
- 提供字体文件 fallback 机制

### 3. 前后端渲染一致性
**难度：⭐⭐⭐⭐⭐**
- Canvas API 在浏览器和 Node.js 可能有细微差异
- 字体渲染差异

**解决方案：**
- 核心算法代码前后端共享（使用 CommonJS）
- 建立视觉回归测试
- 记录关键参数（字体、间距等）

### 4. 性能优化（未来）
**难度：⭐⭐⭐⭐**
- 大批量生成时的并发控制
- 内存管理

**解决方案：**
- 使用消息队列（Bull）
- 限流中间件
- 进程池管理

---

## 📦 依赖清单

### 前端依赖

```json
{
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "fabric": "^5.3.0",
    "antd": "^5.12.0",
    "react-color": "^2.19.3",
    "file-saver": "^2.0.5"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "@vitejs/plugin-react": "^4.2.0"
  }
}
```

### 后端依赖（未来）

```json
{
  "dependencies": {
    "express": "^4.18.2",
    "canvas": "^2.11.2",
    "ali-oss": "^6.18.0",
    "dotenv": "^16.3.1"
  }
}
```

---

## 🎓 参考资料

### Fabric.js 相关
- [Fabric.js 官方文档](http://fabricjs.com/docs/)
- [Fabric.js 中文教程](https://github.com/Rookie-Birds/Fabric-Tutorial_zh-CN)

### Canvas 相关
- [MDN Canvas API](https://developer.mozilla.org/zh-CN/docs/Web/API/Canvas_API)
- [node-canvas 文档](https://github.com/Automattic/node-canvas)

### OSS 相关
- [阿里云 OSS Node.js SDK](https://help.aliyun.com/document_detail/32068.html)

---

## ✅ 需求确认记录

| 需求项 | 确认内容 | 确认时间 |
|--------|---------|---------|
| 印章类型 | 仅公章（圆形） | 2026-02-02 |
| 自定义 Logo | 暂不支持 | 2026-02-02 |
| 拖拽编辑 | 不需要 | 2026-02-02 |
| 导出格式 | PNG | 2026-02-02 |
| 后端接口 | MVP 不实现，预留接口设计 | 2026-02-02 |
| 并发性能 | 暂不优化，未来可能 1w/天 | 2026-02-02 |

---

## 📞 后续待确认事项

- [ ] 印章尺寸规格（是否需要符合国标？）
- [ ] 是否需要保存用户配置历史？
- [ ] 是否需要账号系统？
- [ ] 后端接口的认证鉴权方案
- [ ] OSS 具体使用哪家云服务商？

---

**文档维护者：** AI Assistant  
**最后更新：** 2026-02-02  
**版本：** v1.0
